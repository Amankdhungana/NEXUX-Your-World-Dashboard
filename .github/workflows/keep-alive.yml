name: Keep Supabase Active

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping NEXUX Website
        run: curl -I https://nexux-world.vercel.app/
